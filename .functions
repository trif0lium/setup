#!/usr/bin/env bash

function mkd() {
  mkdir -p "$@" && cd "$_";
}

function tre() {
  tree -aC -I '.git|node_modules' --dirsfirst "$@" | less -FRNX;
}

function en() {
  export $(grep -v '^#' $@ | xargs)
}
